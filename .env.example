# Smart TRA MCP Server Environment Configuration

# Server Configuration  
NODE_ENV=development
PORT=8080
HOST=0.0.0.0

# TDX Taiwan Railway API Credentials
# Register at: https://tdx.transportdata.tw/
TDX_CLIENT_ID=your_client_id_here
TDX_CLIENT_SECRET=your_client_secret_here

# TDX API Endpoints (usually no need to modify)
TDX_AUTH_URL=https://tdx.transportdata.tw/auth/realms/TDXConnect/protocol/openid-connect/token
TDX_BASE_URL=https://tdx.transportdata.tw/api/basic

# Google Cloud Configuration (for production deployment)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GCLOUD_PROJECT=your-gcp-project-id

# CORS Configuration (for production)
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# Example: ALLOWED_ORIGINS=https://n8n.lis186.com,https://app.example.com
ALLOWED_ORIGINS=https://n8n.lis186.com

# Development/Testing Options
# USE_MOCK_DATA=true

# Optional: Future AI parsing features
# GEMINI_API_KEY=your_gemini_api_key_here